{
  "kind": "AutoTemplate",
  "apiVersion": "v1",
  "metadata": {
    "id": "create-ken-burns-zoom-video-clips-from-text-prompts-with-pollinations-and-ffmpeg",
    "name": "Create Ken Burns zoom video clips from text prompts with Pollinations and FFmpeg",
    "description": "## Quick overview\nThis self-hosted workflow collects a text prompt and clip length, generates a 1080×1920 image via Pollinations, then uses local FFmpeg to create a vertical MP4 with a Ken Burns-style slow zoom and returns the output file paths.\n\n## How it works\n1. Receives an image prompt and desired clip length from an n8n form.\n2. Appends cinematic style keywords, URL-encodes the prompt, and builds per-run file paths in a unique `/tmp` folder.\n3. Creates the temporary output folder on disk.\n4. Downloads the AI image from Pollinations using `curl` and saves it to the temp folder.\n5. Uses FFmpeg to crop/scale the image to 1080×1920 and render a zoompan animation into an H.264 MP4 of the requested duration.\n6. Checks FFmpeg’s exit code and returns the image path, video path, prompt, and clip length.\n\n## Setup\n1. Run this workflow on a self-hosted n8n instance with the Execute Command node enabled (it is not available on n8n Cloud).\n2. Install `curl` and `ffmpeg` (with `libx264`) on the n8n host and ensure both are available on the system PATH.\n3. Ensure n8n is allowed to write to the configured temp location (default `/tmp`) and adjust the output directory in the code step if your environment restricts filesystem access.\n\n## Requirements\n- No API key or account anywhere in this workflow - Pollinations is a free, keyless image API\n\n## Customization\n- Change zoom speed or ceiling by editing zoom+0.0012 / 1.15 in the FFmpeg command, or swap in any other keyless image API by editing the URL in the first Code node\n\n## Additional info\nThe zoompan filter's d parameter (frame count) is computed from your requested Clip Length x fps, so the zoom always finishes exactly at the end of the clip regardless of duration - it isn't hardcoded to one fixed length. This template outputs video only, no audio track; pair it with a separate voiceover/TTS template to add sound. Tested end to end: image download and zoom render both verified on a real run before submission.\n\nPair this with Render faceless vertical shorts (https://n8n.io/workflows/17566-render-faceless-vertical-shorts-with-gemini-or-claude-edge-tts-and-ffmpeg/), which chains this same Pollinations + Ken Burns technique together with script generation, TTS voiceover and karaoke captions into one workflow that goes straight from topic to finished vertical video.",
    "category": [
      "automation"
    ],
    "tags": [
      "Content Creation",
      "Multimodal AI",
      "n8n-converted"
    ],
    "author": "Converted from the public n8n template catalog",
    "source": "community",
    "featuredConnectors": [
      "connectionProviders/inlineCode",
      "connectionProviders/request"
    ]
  },
  "workflow": {
    "definition": {
      "$schema": "https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#",
      "contentVersion": "1.0.0.0",
      "triggers": {
        "Manual_Trigger": {
          "type": "Request",
          "kind": "Http"
        }
      },
      "actions": {
        "Image_prompt_and_length_form": {
          "type": "Compose",
          "inputs": {
            "conversionStatus": "unsupported",
            "n8nNodeName": "Image prompt and length form",
            "n8nNodeType": "n8n-nodes-base.formTrigger",
            "message": "Replace this placeholder with an equivalent LAA action."
          },
          "description": "Generated placeholder for an unsupported n8n node.",
          "metadata": {
            "x-n8n-original-parameters": {
              "options": {},
              "formTitle": "Text prompt to AI image + Ken Burns zoom video",
              "formFields": {
                "values": [
                  {
                    "fieldType": "textarea",
                    "fieldLabel": "Image Prompt",
                    "placeholder": "A lighthouse on a cliff at sunset, storm clouds gathering",
                    "requiredField": true
                  },
                  {
                    "fieldType": "number",
                    "fieldLabel": "Clip Length (seconds)",
                    "placeholder": "5",
                    "requiredField": true
                  }
                ]
              },
              "formDescription": "Describe an image and pick a clip length. You get back a vertical MP4 with a slow zoom-in on a free AI-generated image."
            }
          },
          "runAfter": {}
        },
        "Build_the_image_prompt_and_file_paths": {
          "type": "JavaScriptCode",
          "inputs": {
            "code": "const __inputRaw = workflowContext.actions['Image_prompt_and_length_form'].outputs.body;\nconst __inputValues = Array.isArray(__inputRaw) ? __inputRaw : [__inputRaw];\nconst items = __inputValues.map(value =>\n  value && typeof value === 'object' && 'json' in value ? value : { json: value }\n);\nconst __unwrap = value => {\n  if (Array.isArray(value)) {\n    return value.map(item =>\n      item && typeof item === 'object' && 'json' in item ? item.json : item\n    );\n  }\n  return value && typeof value === 'object' && 'json' in value ? value.json : value;\n};\nconst __node = name => {\n  const output = workflowContext.actions[name]?.outputs;\n  const raw = output && typeof output === 'object' && 'body' in output\n    ? output.body\n    : output;\n  const values = Array.isArray(raw) ? raw : [raw];\n  const wrapped = values.map(value =>\n    value && typeof value === 'object' && 'json' in value ? value : { json: value }\n  );\n  return {\n    item: wrapped[0],\n    first: () => wrapped[0],\n    last: () => wrapped[wrapped.length - 1],\n    all: () => wrapped\n  };\n};\n\nconst $json = items[0]?.json ?? {};\nconst $input = {\n  item: items[0],\n  first: () => items[0],\n  last: () => items[items.length - 1],\n  all: () => items\n};\nconst __result = (() => {\n// One folder per run so two runs can never overwrite each other.\nconst runId = ($json.submittedAt || String(Date.now())).replace(/[^a-zA-Z0-9]/g, '');\nconst dir = `/tmp/kenburns-${runId}`;\n\nconst rawPrompt = ($json['Image Prompt'] || '').trim();\nif (!rawPrompt) throw new Error('The Image Prompt field is empty - nothing to generate.');\n\n// Pollinations reads the prompt straight from the URL path, so a few style words\n// appended here go a long way for a faceless-video look without any extra node.\nconst prompt = `${rawPrompt}, cinematic, dark moody lighting, vertical 9:16 composition`;\nconst imageUrl = `https://image.pollinations.ai/prompt/${encodeURIComponent(prompt)}?width=1080&height=1920&nologo=true`;\n\nconst seconds = Number($json['Clip Length (seconds)']) || 5;\nconst fps = 25;\n\nreturn {\n  ...$json,\n  dir,\n  prompt,\n  imageUrl,\n  imagePath: `${dir}/image.jpg`,\n  videoPath: `${dir}/clip.mp4`,\n  seconds,\n  fps,\n  zoomFrames: Math.round(seconds * fps),\n};\n\n})();\nreturn { body: __unwrap(__result) };"
          },
          "runAfter": {
            "Image_prompt_and_length_form": [
              "Succeeded"
            ]
          }
        },
        "Create_the_temp_folder": {
          "type": "Compose",
          "inputs": {
            "conversionStatus": "unsupported",
            "n8nNodeName": "Create the temp folder",
            "n8nNodeType": "n8n-nodes-base.executeCommand",
            "message": "Replace this placeholder with an equivalent LAA action."
          },
          "description": "Generated placeholder for an unsupported n8n node.",
          "metadata": {
            "x-n8n-original-parameters": {
              "command": "=mkdir -p \"{{ $json.dir }}\""
            }
          },
          "runAfter": {
            "Build_the_image_prompt_and_file_paths": [
              "Succeeded"
            ]
          }
        },
        "Download_the_AI_image_Pollinations": {
          "type": "Compose",
          "inputs": {
            "conversionStatus": "unsupported",
            "n8nNodeName": "Download the AI image (Pollinations)",
            "n8nNodeType": "n8n-nodes-base.executeCommand",
            "message": "Replace this placeholder with an equivalent LAA action."
          },
          "description": "Generated placeholder for an unsupported n8n node.",
          "metadata": {
            "x-n8n-original-parameters": {
              "command": "=curl -fsS --retry 3 --retry-delay 5 --retry-all-errors --max-time 120 -o \"{{ $('Build the image prompt and file paths').item.json.imagePath }}\" \"{{ $('Build the image prompt and file paths').item.json.imageUrl }}\""
            }
          },
          "runAfter": {
            "Create_the_temp_folder": [
              "Succeeded"
            ]
          }
        },
        "Render_the_Ken_Burns_zoom_video_FFmpeg": {
          "type": "Compose",
          "inputs": {
            "conversionStatus": "unsupported",
            "n8nNodeName": "Render the Ken Burns zoom video (FFmpeg)",
            "n8nNodeType": "n8n-nodes-base.executeCommand",
            "message": "Replace this placeholder with an equivalent LAA action."
          },
          "description": "Generated placeholder for an unsupported n8n node.",
          "metadata": {
            "x-n8n-original-parameters": {
              "command": "=ffmpeg -y -loop 1 -i \"{{ $('Build the image prompt and file paths').item.json.imagePath }}\" -t {{ $('Build the image prompt and file paths').item.json.seconds }} -vf \"scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,zoompan=z='min(zoom+0.0012,1.15)':d={{ $('Build the image prompt and file paths').item.json.zoomFrames }}:s=1080x1920:fps={{ $('Build the image prompt and file paths').item.json.fps }}\" -c:v libx264 -pix_fmt yuv420p \"{{ $('Build the image prompt and file paths').item.json.videoPath }}\""
            }
          },
          "runAfter": {
            "Download_the_AI_image_Pollinations": [
              "Succeeded"
            ]
          }
        },
        "Verify_the_render_succeeded": {
          "type": "JavaScriptCode",
          "inputs": {
            "code": "const __inputRaw = workflowContext.actions['Render_the_Ken_Burns_zoom_video_FFmpeg'].outputs.body;\nconst __inputValues = Array.isArray(__inputRaw) ? __inputRaw : [__inputRaw];\nconst items = __inputValues.map(value =>\n  value && typeof value === 'object' && 'json' in value ? value : { json: value }\n);\nconst __unwrap = value => {\n  if (Array.isArray(value)) {\n    return value.map(item =>\n      item && typeof item === 'object' && 'json' in item ? item.json : item\n    );\n  }\n  return value && typeof value === 'object' && 'json' in value ? value.json : value;\n};\nconst __node = name => {\n  const output = workflowContext.actions[name]?.outputs;\n  const raw = output && typeof output === 'object' && 'body' in output\n    ? output.body\n    : output;\n  const values = Array.isArray(raw) ? raw : [raw];\n  const wrapped = values.map(value =>\n    value && typeof value === 'object' && 'json' in value ? value : { json: value }\n  );\n  return {\n    item: wrapped[0],\n    first: () => wrapped[0],\n    last: () => wrapped[wrapped.length - 1],\n    all: () => wrapped\n  };\n};\n\nconst $json = items[0]?.json ?? {};\nconst $input = {\n  item: items[0],\n  first: () => items[0],\n  last: () => items[items.length - 1],\n  all: () => items\n};\nconst __result = (() => {\nconst cmdResult = $json;\nconst paths = __node('Build_the_image_prompt_and_file_paths').item.json;\nif (cmdResult.exitCode !== undefined && cmdResult.exitCode !== 0) {\n  throw new Error('FFmpeg failed: ' + (cmdResult.stderr || '').slice(-500));\n}\nreturn { json: { videoPath: paths.videoPath, imagePath: paths.imagePath, prompt: paths.prompt, seconds: paths.seconds } };\n\n})();\nreturn { body: __unwrap(__result) };"
          },
          "runAfter": {
            "Render_the_Ken_Burns_zoom_video_FFmpeg": [
              "Succeeded"
            ]
          }
        }
      },
      "outputs": {}
    }
  },
  "trigger": {
    "name": "Manual_Trigger",
    "outputs": {
      "method": "POST",
      "headers": {
        "Content-Type": "application/json"
      },
      "queries": {},
      "body": {}
    }
  },
  "mocks": {},
  "connections": {}
}
